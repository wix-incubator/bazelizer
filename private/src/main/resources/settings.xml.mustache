<?xml version="1.0" encoding="UTF-8"?>
<!-- AUTOGENERATED SETTINGS XML FILE. DO NOT EDIT. -->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
    <localRepository>{{ localRepository }}</localRepository>
    <profiles>
        {{#profiles}}
        <profile>
            <id>{{id}}</id>
            <repositories>
                <repository>
                    <snapshots><enabled>false</enabled></snapshots>
                    <id>{{id}}_repo</id>
                    <name>Deps repository for {{id}}</name>
                    <url>{{url}}</url>
                </repository>
            </repositories>
            <pluginRepositories>
                <pluginRepository>
                    <id>{{id}}_plugin</id>
                    <name>Plugins repository for {{id}}</name>
                    <url>{{url}}</url>
                </pluginRepository>
            </pluginRepositories>
        </profile>
        {{/profiles}}
    </profiles>

    <activeProfiles>
        {{#profiles}}
        <activeProfile>{{id}}</activeProfile>
        {{/profiles}}
    </activeProfiles>
</settings>